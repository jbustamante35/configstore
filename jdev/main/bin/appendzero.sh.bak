#!/bin/bash

num=$1
app=$2
arg=%0${num}d\.${app}
echo "args: $arg"

for x in *$app ; do
    inp=%0$num
    ext=${x%$app} 
    y=$(printf ${inp}d$app $ext)
    if [ $x != $y ]; then
        #mv $x $y
        echo "mv $x | $y"
    fi
done

echo "Finished renaming all $app files"
