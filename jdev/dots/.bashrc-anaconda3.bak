# .bashrc

# Source global definitions
if [ -f /etc/bashrc ]; then
	. /etc/bashrc
fi

# User specific aliases and functions
alias ll="ls -shao"
alias l="ls -a"
export EDITOR='vim'

# Shortcut for checking highest 20 items on disk
alias duA='sudo du -ah /home | sort -nr | head -n 20'
